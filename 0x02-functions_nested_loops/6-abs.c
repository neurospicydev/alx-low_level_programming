#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer
 * @num: The integer to compute
 *
 * Return: Always 0 (Success)
 */

int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}

	else
	{
		return (-num);
	}
}
